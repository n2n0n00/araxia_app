import { View, Text, ScrollView, SafeAreaView } from "react-native";
import React from "react";

const Feed = () => {
  return (
    <View className="items-center justify-center h-screen w-screen">
      <Text className="font-mbold text-3xl text-black">Hello</Text>
    </View>
  );
};

export default Feed;
